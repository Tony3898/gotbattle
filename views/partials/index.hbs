{{>header project=project}}
<div class="container-fluid">
    <div class="row">
        <div class="col-sm-12">
            <div class="intro">
                <h1 class="text-center">Hello {{#if loggedIn}} <span id="username"></span> {{else}}<span>there</span>{{/if}}, welcome to my
                    project <span class="heart" style="text-transform: uppercase">{{project}}</span></h1>
                <a href="{{digInUrl}}" class="heart"><h1>Let's dig in</h1></a>
            </div>
        </div>
    </div>
</div>
<script src="/public/assets/js/index.js" type="text/javascript"></script>
{{>footer}}